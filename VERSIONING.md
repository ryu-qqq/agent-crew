# 버전·릴리스 정책

## 버전 규칙 (SemVer)

agent-crew는 git tag로 버전을 끊습니다. 별도 버전 관리 시스템은 두지 않습니다.

`vMAJOR.MINOR.PATCH`

| 증가 | 언제 |
|---|---|
| **MAJOR** | 소비 프로젝트가 `.claude/project.yaml`을 고쳐야 하는 변경 (스키마 변경, 자산 제거·개명) |
| **MINOR** | 자산 추가, 하위호환되는 동작 개선 |
| **PATCH** | 문구 교정, 버그 수정 — 동작·스키마 영향 없음 |

태그를 끊을 때 `releases/vX.Y.Z.md`를 작성하고 `CHANGELOG.md` 인덱스에 한 줄을 추가합니다.
→ 아래 [릴리스 기록](#릴리스-기록) 참고.

## 릴리스 기록

에이전트·스킬은 소중한 자산입니다. 버전마다 "무엇이 바뀌었나"를 넘어, **무엇이·왜 좋아졌고 어떻게 검증됐나**까지 남깁니다.

- 버전마다 `releases/vX.Y.Z.md` 파일을 둡니다. 양식은 `releases/TEMPLATE.md`입니다.
- `CHANGELOG.md`는 버전당 한 줄 인덱스입니다. 전체 기록은 `releases/`에 있습니다.
- 릴리스 파일이 담는 내용:
  - **자산별 변경·개선** — 무엇이 바뀌고, 이전 대비 *왜* 더 나은가
  - **검증 (ops-pilot)** — 어떤 시나리오로, 모델별로 어떤 결과였나, 회귀는 없나
  - **호환성** — project.yaml 영향·마이그레이션

> agent-crew는 평가를 *실행*하지 않습니다 (ops-pilot의 몫입니다). 릴리스 파일의 "검증" 섹션은 ops-pilot 검증 *결과의 기록*입니다 — "평가 기능"이 아니라 버전 메타데이터 문서입니다.

### 검증 후 태그

태그된 버전은 사실상 불변입니다. 검증되지 않은 자산에 태그가 붙지 않도록 다음 순서를 따릅니다.

1. 릴리스 커밋을 main에 올립니다 (`releases/vX.Y.Z.md` 포함, "검증" 섹션은 "미검증" 상태).
2. ops-pilot으로 그 시점 자산을 검증합니다.
3. 검증 결과를 `releases/vX.Y.Z.md`의 "검증" 섹션에 채워 커밋합니다.
4. 그 커밋에 `git tag -a vX.Y.Z`를 붙입니다.

즉 **태그는 검증 기록이 채워진 커밋을 가리킵니다.** 빠른 수정(PATCH)이라 검증을 건너뛸 때는 "검증 상태: 미검증"을 명시한 채 태그합니다 — 생략은 가능하되 흔적은 남깁니다.

## 적용·동기화

소비 프로젝트로 자산을 가져가는 방식 — 동기화·버전 핀 고정·적용 — 은 ops-pilot이 담당합니다. agent-crew는 자산을 만들고 git tag로 버전을 끊는 데까지만 책임집니다.
→ [README](./README.md)의 "생태계" 참고.
